# Hermes Web UI -- minimal Python dependencies
# The server uses only stdlib + pyyaml.
# All heavy ML/agent deps live in the Hermes agent venv.
pyyaml>=6.0
# Optional: enables full Jinja2-powered prompt templates. Without it,
# api/prompt_builder.py falls back to a simple {{var}} substitution.
jinja2>=3.1

